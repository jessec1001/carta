/*
 * We have multiple interacting components here.
 * - For each timeline item, we render a bullet point and a line through the bullet point.
 * - If the timeline should trail at the beginning or end, a line that fades should be rendered with a fixed size.
 */

import { FunctionComponent } from "react";

const Timeline: FunctionComponent = () => {
  return null;
};
